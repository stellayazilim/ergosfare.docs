---
title: Ergosfare
description: A modular mediator for CQRS-style messaging in .NET — commands, queries and events over one pipeline model.
template: splash
hero:
  tagline: A modular mediator for CQRS-style messaging in .NET. The v2 preview is where active development happens.
  image:
    file: ../../assets/ergosfare.svg
  actions:
    - text: Start on v2 preview
      link: /ergosfare.docs/preview/getting-started/introduction
      icon: right-arrow
      variant: primary
    - text: v1 stable docs
      link: /ergosfare.docs/getting-started/introduction
      icon: document
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';
import { StarField } from "../../components/constellation/starfield.tsx"

<div className="pt-20 pb-16 sm:pt-32 sm:pb-20 lg:py-20">
  <div className="relative">
    <StarField client:only className="-top-48 -right-44" />
  </div>

<CardGrid stagger>
  <Card title="Two lines, one library" icon="seti:c-sharp">
    **v1 stable** is what you get from a plain `dotnet add package`. **v2 preview** is the
    active line — same concepts, a much faster implementation, and close to stable.
    [Compare them](/ergosfare.docs/getting-started/introduction#how-v1-and-v2-differ).
  </Card>

  <Card title="Compile-time registration" icon="rocket">
    In v2, a Roslyn generator discovers handlers in your compilation *and* in referenced
    assemblies and emits the registration for you — no startup assembly scan, no reflection.
    [Read more](/ergosfare.docs/preview/core-concepts/compile-time-discovery).
  </Card>

  <Card title="Allocation you can account for" icon="approve-check">
    v2's pooled execution contexts and `ValueTask`-first surface put 100k sequential
    dispatches at 2.3 MB, against MediatR's 18.3 MB for the same loop.
    [See the benchmarks](/ergosfare.docs/preview/advanced/performance).
  </Card>

  <Card title="Interceptor pipeline" icon="layers">
    Pre, post, exception and final stages for logging, validation, authorization and cleanup
    — attached without touching handlers, and matched covariantly across a type hierarchy.
  </Card>

  <Card title="Native AOT and trimming" icon="setting">
    v2 references every construct statically and anchors every generic instantiation,
    `record struct` messages included.
    [How it works](/ergosfare.docs/preview/advanced/aot-and-trimming).
  </Card>

  <Card title="Modular" icon="puzzle-piece">
    Commands, Queries and Events are independent packages over a shared core. Register only
    the modules you use — and take only the mediator interface each component needs.
  </Card>
</CardGrid>
</div>
